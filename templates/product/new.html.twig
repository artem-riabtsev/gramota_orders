{% extends 'base.html.twig' %}

{% block title %}Новый продукт{% endblock %}

{% block content %}
<h1>Новый продукт</h1>

<div class="card">
    <div class="card-body">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        
        <button class="btn btn-success">Создать</button>
        <a href="{{ app.request.headers.get('referer') ?: path('app_product_index') }}" class="btn btn-secondary ms-1">
            Назад
        </a>

        {{ form_end(form) }}
    </div>
</div>
{% endblock %}